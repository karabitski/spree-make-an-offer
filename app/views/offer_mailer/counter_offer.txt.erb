You received a counter offer of <%= Spree::Money.new(@offer.price).to_s %> for the <%= link_to @offer.product.name, product_path(@offer.product) %> .
You can accept this offer by clicking <%= link_to 'here', Rails.env == 'development' ? "http://localhost:3000/accept-counter-offer/#{@offer.id}" : "#{Spree::Config.site_url}/accept-counter-offer/#{@offer.id}" %>.
